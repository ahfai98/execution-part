export with no arguments will print out declare -x NAME=doublequoteVALUEdoublequote
if value = 0, will just print NAME
if value = empty string, will print NAME=""
sort based on their names in alphabet orders, underscore and lowercase first

export (NAME)if do not have = sign, 
value will be zero
1. if key not found, will just add NAME to environment list, no equal sign
2. if key already exist, do not do anything

export (NAME) with equal sign will set value
if there is equal sign but no value, set value to empty string
1. if key exists, update value
2. if key does not exist, add to environment list


Unset needs to free the t_env for del

expand_env returns NULL? need to free?






